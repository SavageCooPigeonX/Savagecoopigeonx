# Final sanitization pass to ensure all problematic characters are removed or replaced
def final_sanitize_text(text):
    return (text.replace("–", "-")
                .replace("—", "-")
                .replace("‘", "'")
                .replace("’", "'")
                .replace("“", '"')
                .replace("”", '"')
                .replace("•", "-")
                .replace("…", "..."))

# Sanitize the text and regenerate the covenant PDF
final_clean_text = final_sanitize_text(sacred_covenant_text)
sacred_pdf_path = generate_pdf(final_clean_text, "Sacred_Covenant_SavageOS.pdf")

sacred_pdf_path